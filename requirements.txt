# Cybersecurity Multi-Agent Learning Loop Model
# Python dependencies

# LLM Providers — install the one matching your LLM_PROVIDER setting
anthropic>=0.28.0          # For LLM_PROVIDER=anthropic (default)
# openai>=1.30.0           # For LLM_PROVIDER=openai (uncomment if using OpenAI)

# CLI and utilities
python-dotenv>=1.0.0       # Load .env files for API key management
pyyaml>=6.0.1              # Feedback record YAML parsing

# Optional: orchestration frameworks (choose one if desired)
# langchain>=0.2.0         # LangChain multi-agent orchestration
# crewai>=0.30.0           # CrewAI role-based agent framework
# autogen>=0.2.0           # Microsoft AutoGen

# Development and testing
pytest>=8.0.0
pytest-cov>=5.0.0
